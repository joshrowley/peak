var stylus = require('stylus');

var Stylus = {

  kind: 'css',
  extension: 'styl',

  before_compile: function (options, resolve, reject) {
    // before
  },

  compile: function (options, resolve, reject) {
    // exec
  },

  after_compile: function (options, resolve, reject) {
    // after
  }

}

module.exports = Haml;
